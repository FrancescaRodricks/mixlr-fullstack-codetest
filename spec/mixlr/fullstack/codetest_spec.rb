RSpec.describe Mixlr::Fullstack::Codetest do
  it "has a version number" do
    expect(Mixlr::Fullstack::Codetest::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
